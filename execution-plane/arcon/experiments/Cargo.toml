[package]
name = "experiments"
version = "0.1.0"
authors = ["Max Meldrum <mmeldrum@kth.se>"]
edition = "2018"

[dependencies]
arcon = { path = "../", features = ["thread_pinning"]}
clap = "2.33.0"
num_cpus = "1.10.1"
# Should not need to do this..
# But for now..
abomonation = "0.7.3"
prost = { git = "https://github.com/Max-Meldrum/prost.git", branch = "derive_generics" }
